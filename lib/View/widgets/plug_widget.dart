import 'package:flutter/material.dart';

class PlugWidget extends StatelessWidget {
  const PlugWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: const Placeholder());
  }
}
